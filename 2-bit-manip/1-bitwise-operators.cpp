/*
Bitwise operators 👇
&  -> AND
|  -> OR
^  -> XOR
~  -> One's complement(Flipping all the bits and adding one)
<< -> Left Shift
>> -> Right Shift
*/

// a<<b == a * pow(2,b) -> Left shift does multiplication
// a>>b == a / pow(2,b) -> Right shift does divides